{% extends 'base.html.twig' %}
{% block title %}{{ trick.slug }}{% endblock %}
{% block metadesc %}{% endblock %}
{% block metatitle %}{% endblock %}

{% block body %}

    <div class="container">
        <img src="https://picsum.photos/1024/502" class="card-img-top" alt="...">
        <h1 class="title-trick">{{ trick.title }}</h1>
    </div>
    <div class="container flex test">
        {% for key,image in trick.images  %}
            <a href="javascript(void)" class=" col-sm- col-md-2  " onclick="MoveOnCover('{{ image.path }}')"><img src="{{ asset('uploads/') }}{{ image.path }}" class="card-img-top trick_item" alt="..."></a>
        {% endfor %}
        {% for key,video in trick.videos  %}
            <a  target="_blank" href="{{ video.uri }}" class=" col-sm- col-md-2  "><img src="{{ asset('uploads/video-logo.jpg') }}" class="card-img-top trick_item"></a>
        {% endfor %}
    </div>
    <div class="container my-3">
        {{ trick.description }}
    </div>

    <div class="container my-3">
        <div class="col-sm-2 text-center">Auteur</div>

        <div class="col-sm-2 text-center">{{ trick.group.name}}</div>

        <div class="col-sm-2 text-center">{{ trick.dateAdd|date('Y-m-d') }}</div>

        <div class="col-sm-2 text-center">{{ trick.dateUpdate|date('Y-m-d') }}</div>
    </div>

    <div class="container mx-auto row my-3 ">
        {% for comment in trick.comments  %}
            <div class=" col-sm-12 trick">
                {#
                <h3>{{ comment.dateAdd }}</h3>
                #}
                <p class="text-center">{{ comment.comment }}</p>
            </div>
        {% endfor %}
    </div>
{% endblock %}
