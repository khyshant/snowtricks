<div id="image-{{ form.vars.name }}">
    {{ form_row(form.uploadedFile) }}
    <button class="btn btn-danger btn-remove" type="button" data-target="#image-{{ form.vars.name }}">
        Supprimer
    </button>
</div>